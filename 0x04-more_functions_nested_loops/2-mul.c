#include <main.h>
/**
 * _isdigit - determine if digit
 *
 * @c: character to determine
 *
 * Return: gives boolean
 */
int mul(int a, int b)
{
	int answer;
	answer = a * b;
	printf("%d \n", answer)

	return (0);
}
